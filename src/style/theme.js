import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        type: 'light',
        primary: {
            main: '#5F7161',
        },
        secondary: {
            main: '#78909c',
        },
        background: {
            default: '#D5D8B5',
        },
    },
})

export default theme;